refactor(make): auto-activate venv inside dev target

Remove VIRTUAL_ENV check that caused "unbound variable" error.
Makefile now uses venv Python directly without checking shell state.

- Replace conditional VIRTUAL_ENV check with direct venv/bin/python usage
- Add automatic port cleanup before dev startup
- Ensure make dev works from fresh clone without manual activation

Tests: Added smoke test to verify no bash errors in clean environment