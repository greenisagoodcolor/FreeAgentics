aiofiles==24.1.0
alembic==1.16.2

# Code Quality
black==25.1.0

# Test Infrastructure Dependencies
boto3>=1.35.0,<2.0.0  # For MinIO S3 compatibility testing
faker==33.1.0
# Minimal CI requirements for fast pipeline feedback
# Core Framework (required for API)
fastapi==0.115.14
flake8==7.3.0

# Web and API
httpx==0.28.1
inferactively-pymdp==0.0.7.1  # CRITICAL: Core Active Inference functionality (imports as 'pymdp')
isort==6.0.1
lxml==5.4.0  # Required for mypy HTML reports (5.4.1 doesn't exist)
mypy==1.16.1

# Basic Scientific Computing (minimal)
numpy==2.2.1
passlib==1.7.4  # CRITICAL: Required for auth/security_implementation.py
pydantic==2.9.2

# Authentication (required for tests)
pyjwt==2.10.1

# Testing
pytest==8.4.1
pytest-asyncio==1.0.0
pytest-cov==6.2.1
python-dotenv==1.1.1
python-multipart==0.0.20

# Configuration
pyyaml==6.0.2
scipy==1.16.0  # CRITICAL: Required for agents/memory_optimization/

# Database (minimal)
sqlalchemy==2.0.41
starlette==0.46.2
toml==0.10.2
uvicorn==0.35.0
